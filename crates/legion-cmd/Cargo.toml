[package]
name = "legion-cmd"
version = "0.1.0"
edition = "2024"
description = "Pure command router for legion's PreToolUse enforcement (#1058)"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "0.8"
thiserror = "2"
